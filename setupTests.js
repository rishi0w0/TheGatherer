// Optional: Configure or set up a testing framework before each test
// For example, you can set global variables or configure testing libraries

// Example: Setting up Jest-DOM for better assertions
require('@testing-library/jest-dom/extend-expect');

// Example: Mock global variables if needed
global.someGlobalVariable = 'mocked value';
